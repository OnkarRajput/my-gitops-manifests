apiVersion: v1
kind: Service
metadata:
	name: enterprise-service
	namespace: default
spec:
	type: NodePort
	selector:
		app: enterprise-web
	ports:
		- port: 80
			targetPort: 80
			nodePort: 32080 # Maps the app to port 32080 on your Minikube IP
