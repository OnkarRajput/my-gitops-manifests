apiVersion: v1
kind: Service
metadata:
  name: enterprise-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: enterprise-web
  ports:
    - port: 9999
      targetPort: 9999
      nodePort: 32080
